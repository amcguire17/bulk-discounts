<h1><%= @discount.name %></h1>
<h3>Discount Percentage: <%= number_to_percentage(@discount.percentage, precision: 0) %></h3>
<h3>Discount Quantity Threshold: <%= @discount.quantity %></h3>

<%= link_to 'Edit Discount', edit_merchant_bulk_discount_path(@merchant, @discount) %>
