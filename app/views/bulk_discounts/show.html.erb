<h1>Discount Percentage: <%= number_to_percentage(@discount.percentage, precision: 0) %></h1>
<h1>Discount Quantity Threshold: <%= @discount.quantity %></h1>

<%= link_to 'Edit Discount', edit_merchant_bulk_discount_path(@merchant, @discount) %>
